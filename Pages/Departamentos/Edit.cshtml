@page "{id:int}"
@model Portal_Refeicoes.Pages.Departamentos.EditModel
@{
    ViewData["Title"] = "Editar Departamento";
}

<div class="container mt-4" style="max-width: 600px;">
    <div class="card shadow">
        <div class="card-header bg-primary text-white">
            <h4 class="mb-0"><i class="bi bi-pencil-square"></i> Editar Departamento</h4>
        </div>
        <div class="card-body">
            <form method="post">
                <input type="hidden" asp-for="Departamento.Id" />
                <div class="mb-3">
                    <label asp-for="Departamento.Nome" class="form-label fw-bold">Nome do Departamento</label>
                    <input asp-for="Departamento.Nome" class="form-control" />
                    <span asp-validation-for="Departamento.Nome" class="text-danger"></span>
                </div>

                <div class="d-flex justify-content-end gap-2 mt-4">
                    <a asp-page="Index" class="btn btn-outline-secondary">Cancelar</a>
                    <button type="submit" class="btn btn-primary px-4">Salvar Alterações</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}